# Plan: Day 2 말발굽길 이미지 캐러셀

## 상태: 완료

## 요구사항
- Day 2 섹션에서 "🚶 도보 코스 ② — 4코스 고사리밭길 (일부, 가인리까지)" 카드와
  "🏠 숙소" 블록 사이에, Day 3와 같은 방식(카로셀)으로 이미지들을 추가한다.
- 이미지: 남해바래길 공식 홍보 자료(5코스 말발굽길 편) 13장, **파일명 순서대로** 배치
  (KakaoTalk_Photo_..._001 ~ 013 순서 = 001 코스 정보 카드 → 002 거리 안내/타이틀 →
  003 창선교(힐링포인트①) → 004 추섬공원(힐링포인트②) → 005 보현사(코스①) →
  006 지족해협죽방렴(코스②) → 007 포구 풍경 → 008 마을길 풍경 → 009 말발굽길 전경 3컷 →
  010 숲길/해안길 인셋 2컷 → 011 인생샷(사람) → 012 힐링 포인트(바위) →
  013 마무리(적량마을 시작점, 말 일러스트))
- Day 3에서 만든 캐러셀 컴포넌트(`assets/carousel.js`, CSS)를 그대로 재사용한다
  (요구사항이 "같은 방식으로"이므로 새 컴포넌트를 만들지 않고 기존 것을 재사용).

## 접근 방식
1. 캐러셀 핵심 로직은 Day 3에서 이미 `assets/carousel.js`에 순수 함수로 분리되어
   `tests/carousel.test.js`로 테스트되어 있음 — 인덱스 길이(slide 개수)에 무관하게 동작하는
   범용 구현이므로 새 로직 추가 없이 그대로 재사용 가능.
2. TDD: 이번 기능이 기존 로직에 의존하는 "재사용"이므로, 이번 변경에서 실제로 새로 검증해야
   하는 것은 "한 페이지에 서로 다른 개수의 슬라이드를 가진 캐러셀 두 개가 동시에 존재해도
   서로 간섭 없이 동작하는가"이다. 이를 위해:
   - `tests/carousel.test.js`에 13개 슬라이드 케이스(이번 기능의 실제 슬라이드 수)에 대한
     경계값 테스트를 추가한다 (RED로 만들 새 버그는 없지만, 이번 슬라이드 수에 대한 회귀
     테스트를 명시적으로 남겨 향후 리팩터링 시 안전망이 되도록 함).
   - Playwright로 실제 `index.html`을 열어 Day 2/Day 3 캐러셀 두 개가 각각 독립적으로
     동작하는지 통합 테스트한다 (한쪽 다음 버튼이 다른 쪽에 영향 주지 않는지 등).
3. `index.html`의 Day 2 섹션, "🚶 도보 코스 ② — 4코스 고사리밭길" 카드와 "🏠 숙소" 사이에
   `id="day2-carousel"` 캐러셀 마크업 삽입 (Day 3와 동일한 `.carousel` 구조 재사용).
4. 이미지 13장을 웹용으로 리사이즈(900px 폭)/압축(JPEG quality 78) 후
   `assets/day2-carousel/slide-01.jpg` ~ `slide-13.jpg`로 저장, 파일명 순서(=001~013 원본
   순서)를 그대로 유지.
5. `node --test` + Playwright 통합 스모크 테스트로 확인.
6. 계획 문서 업데이트, 커밋 메시지 작성.

## 변경 파일
- `tests/carousel.test.js` — 13-슬라이드 경계값 테스트 추가
- `index.html` — Day 2 섹션에 캐러셀 마크업 추가 (`id="day2-carousel"`)
- `assets/day2-carousel/slide-01.jpg` ~ `slide-13.jpg` (신규)

## 진행 상황
- [x] 13-슬라이드 경계값 단위 테스트 추가 및 통과 확인 — `tests/carousel.test.js`에
      `nextIndex`/`prevIndex`의 13-슬라이드 마지막↔처음 순환, `indexFromScroll`의 13-슬라이드
      상한 clamp, 서로 다른 길이(11 vs 13)를 번갈아 호출해도 상태가 섞이지 않는지 확인하는
      테스트 추가. `node --test` 14/14 통과 (기존 10개 + 신규 4개)
- [x] index.html에 Day 2 캐러셀 삽입 + 이미지 추가 (파일명 순서 유지) — "🚶 도보 코스 ② —
      4코스 고사리밭길" 카드와 "🏠 숙소" 사이에 "📸 말발굽길 미리보기" 캐러셀 삽입.
      이미지 13장을 `assets/day2-carousel/slide-01.jpg`~`slide-13.jpg`로 웹용
      리사이즈(900px 폭)/압축 후, 업로드된 파일명 번호(001~013) 순서 그대로 배치
- [x] node --test 통과 확인 (14/14)
- [x] Playwright로 Day2/Day3 캐러셀 동시 동작 확인:
      - DOM 순서: 코스① → 코스② → "📸 말발굽길 미리보기" → 숙소 (요구사항대로)
      - Day2 13슬라이드/13점, Day3 11슬라이드/11점 정상 렌더링
      - Day2에서 다음 버튼 2번 클릭 → Day2만 인덱스 2로 이동, Day3는 0에 그대로 유지
      - Day2 점 인디케이터로 13번째 슬라이드로 바로 이동 정상 동작
      - 이어서 Day3 다음 버튼 클릭 → Day3만 인덱스 1로 이동, Day2는 12번째 슬라이드 그대로 유지
        (두 캐러셀이 서로 완전히 독립적으로 동작함을 확인)
      - 이미지 지연 로딩(loading="lazy") 정상 동작 확인 (뷰포트 근처 슬라이드만 우선 로드)
- [x] 계획 문서 업데이트
- [x] 커밋 메시지 작성 (아래 참고)

## 커밋 메시지
```
feat: Day 2 말발굽길 이미지 캐러셀 추가

- 도보 코스② 카드와 숙소 안내 사이에 남해바래길 5코스(말발굽길) 공식
  홍보 이미지 13장을 파일명 순서 그대로 캐러셀로 추가
- Day 3에서 만든 캐러셀 컴포넌트(assets/carousel.js)를 그대로 재사용
- 13-슬라이드 경계값 테스트와, 슬라이드 개수가 다른 두 캐러셀이 서로
  간섭하지 않는지 확인하는 테스트를 tests/carousel.test.js에 추가
- Playwright로 실제 페이지에서 두 캐러셀의 독립 동작을 스모크 테스트
```
